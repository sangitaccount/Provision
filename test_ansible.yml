- hosts: test_ansible 
  gather_facts: no
  become_user: anser
  connection: ssh

  tasks:
  - name: "Print Date"
    shell: date
    register: date_output

  - debug: var=date_output

  - name: "Install htop package"
    yum: pkg=htop state=installed
